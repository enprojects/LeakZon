

namespace Leakzone.Backend.Infrastructure
{
    public static  class EnumerableHelper
    {
        //public static IEnumerable<IEnumerable<T>> ChunkSplitter<T>(this IEnumerable<T> source, int size, Action<IEN>)
        //{
        //    var skip = 0;
        //    while (source.Count() > skip)
        //    {
        //        yield return source.Skip(skip).Take(size);
        //        skip += size;
        //    }
        //}
    }
}
